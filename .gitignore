.DS_Store
.build/
.swiftpm/
DerivedData/
*.xcuserstate
xcuserdata/

# Generated by `xcodegen generate` from App/project.yml — that file is the
# source of truth, so the project itself is not tracked.
App/*.xcodeproj

# Archives, exported .ipa and other release artifacts.
build/

# Private development notes — see dev/NOTES.md. Kept out of the public repo
# deliberately; the directory is its own git repository.
dev/
